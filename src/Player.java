package blackjackgame;
import java.util.Random;
public static String Player(playerCard) inherits Game{
    int playerValue = 0;
    Random cardSelect = new Random
    int maxCardValue = 12;
    int firstCard = cardSelect.nextInt(maxCardValue);
    char aceCard = "A";
    int nextCard = cardSelect.nextInt(maxCardValue);
    String playerCard = ""
    switch (firstCard) {
        case firstCard == 0:
            firstCard = 1;
            playerCard = playerCard + "1";
            break;
        case firstCard == 11
        playerCard +=  "A"
        case
        default:
            playerCard = firstCard + "" + secondCard;
            break;
    }
    return playerCard;
} 